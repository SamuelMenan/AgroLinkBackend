server:
  port: ${PORT:8080}

spring:
  application:
    name: agrolink-backend

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    root: INFO
    com.agrolink.infrastructure.supabase: DEBUG
